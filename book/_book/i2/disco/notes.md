# Notes

There are separate tables for an item's notes. For example as well as the IS_DATA.Properties table, there is a IS_DATA.Properties_N table.

To add notes to a record via the api:

1. Submit note changeset added notes to existing item postman

2. Solr get the version
